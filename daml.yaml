sdk-version: 1.2.0
name: daml-ui-template
source: daml
parties:
  - Alice
  - Bob
  - Administrator
version: 0.0.1
init-script: Main:initializePeople
start-navigator: true
sandbox-options:
  - --ledgerid=daml-ui-template
  - -w
dependencies:
  - daml-prim
  - daml-stdlib
  - daml-script
json-api-options:
  - --query-store-jdbc-config=driver=org.postgresql.Driver,url=jdbc:postgresql://localhost:5432/daml_ui_template,user=daml_json_client,password=password,createSchema=false
    #                          driver=org.postgresql.Driver,url=jdbc:postgresql://localhost:5432/test?&ssl=true,user=postgres,password=password,createSchema=false

